# The application ID used to to uniquely store session and cache data, mutex locks, and more
CRAFT_APP_ID=

# The environment Craft is currently running in (dev, staging, production, etc.)
CRAFT_ENVIRONMENT=dev

# The secure key Craft will use for hashing and encrypting data
CRAFT_SECURITY_KEY=

# Database connection settings
CRAFT_DB_DRIVER=mysql
CRAFT_DB_SERVER=127.0.0.1
CRAFT_DB_PORT=3306
CRAFT_DB_DATABASE=
CRAFT_DB_USER=root
CRAFT_DB_PASSWORD=
CRAFT_DB_SCHEMA=public
CRAFT_DB_TABLE_PREFIX=

# General settings (see config/general.php)
SITE_NAME="Tilt Boilermaker"
SITE_EMAIL="webbies@tiltbuilt.com"
DEV_MODE=true
ALLOW_ADMIN_CHANGES=true
DISALLOW_ROBOTS=true
CRAFT_WEB_ROOT=""
MAILPIT_SMTP_PORT="1025"
PRIMARY_SITE_URL=""
MAILPIT_SMTP_HOSTNAME="127.0.0.1"
VITE_USE_DEV_SERVER=1


IMAGE_UPLOADS_URL="${PRIMARY_SITE_URL}/${UPLOADS_FOLDER}/images"
IMAGE_UPLOADS_PATH="${SITE_PATH}${PUBLIC_FOLDER}/${UPLOADS_FOLDER}/images"

DOC_ASSETS_URL="${PRIMARY_SITE_URL}/${UPLOADS_FOLDER}/docs"
DOC_ASSETS_PATH="${SITE_PATH}${PUBLIC_FOLDER}/${UPLOADS_FOLDER}/docs"

CONTENT_VIDEO_ASSETS_URL="${PRIMARY_SITE_URL}/${UPLOADS_FOLDER}/contentvideos"
CONTENT_VIDEO_ASSETS_PATH="${SITE_PATH}${PUBLIC_FOLDER}/${UPLOADS_FOLDER}/contentvideos"

BG_VIDEO_ASSETS_URL="${PRIMARY_SITE_URL}/${UPLOADS_FOLDER}/bgvideos"
BG_VIDEO_ASSETS_PATH="${SITE_PATH}${PUBLIC_FOLDER}/${UPLOADS_FOLDER}/bgvideos"

# IMG IX 
IMGIX_SRC_DOMAIN="website.imgix.net"
IMGIX_API_KEY=""
IMGIX_TOKEN=""

# Postmark for Email Service
PM_API_TOKEN=""
PM_MESSAGE_STREAM="broadcast"

# SERVD
SERVD_PROJECT_SLUG=""
SERVD_SECURITY_KEY=""